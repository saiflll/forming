# Database Configuration (via SSH Tunnel)
# PENTING: Pakai 127.0.0.1 bukan localhost (avoid IPv6 issue)
DB_HOST=172.20.100.11
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password_rahasia_anda
DB_NAME=servfi

# MQTT Configuration (via SSH Tunnel with Authentication)
# PENTING: Pakai 127.0.0.1 bukan localhost (avoid IPv6 issue)
MQTT_HOST=172.20.100.11
MQTT_PORT=1883
MQTT_USER=your_mqtt_user
MQTT_PASSWORD=your_mqtt_password

# Google Sheets Integration (Optional)
# Follow GOOGLE-SHEETS-SETUP.md for setup instructions
GOOGLE_SHEETS_CREDENTIALS=
GOOGLE_SPREADSHEET_ID=
GOOGLE_SHEET_NAME=Production Data

# App Configuration
TZ=Asia/Jakarta
